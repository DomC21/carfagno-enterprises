{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "buildCommand": "pnpm install && pnpm build",
  "installCommand": "pnpm install",
  "framework": "vite",
  "trailingSlash": false,
  "cleanUrls": true,
  "routes": [
    { "handle": "filesystem" },
    { "src": "/coaching", "dest": "/index.html", "status": 200 },
    { "src": "/projects/(.*)", "dest": "/index.html", "status": 200 }
  ]
}
